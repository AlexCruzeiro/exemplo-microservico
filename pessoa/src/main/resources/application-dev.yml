server:
  port: 8082

spring:
  application:
    name: PESSOA-SERVICE

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://localhost:8761/eureka/
  instance:
    hostname: localhost